{
    "type": "object",
    "primaryKey":"id",
    "required": ["name"],
    "properties": {
        "id": {
            "type": "number"
        },
        "name": {
            "type":"string",
            "readOnly": true
        },
        "created_at": {
            "type": ["string", "null"],
            "format": "date-time",
            "readOnly": true,
            "instanceof": "Date",
            "default": null
        }
    }
}
